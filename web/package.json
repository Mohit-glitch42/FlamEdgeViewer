{
  "name": "flam-edge-viewer-ready",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --port 5000 --host"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "typescript": "^5.5.0"
  }
}